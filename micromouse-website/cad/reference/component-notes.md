# Micromouse asset notes

Complete these details before replacing the procedural digital twin with the production GLB.

- CAD source application/version:
- Real width × length × height:
- Forward direction in the CAD scene:
- Showcase input: mouse / touch / both:
- Monitor resolution and orientation:
- Showcase computer/GPU:
- Required team title and UNSW branding:
- Components that should not be shown:

## Interactive component copy

The application contract expects the mesh names listed in `src/config/components.ts`. Record any intentional naming differences here before export.
